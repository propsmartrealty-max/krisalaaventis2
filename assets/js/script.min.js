(function(){'use strict';const SOVEREIGN_VERSION = '3.0';const ERROR_LOG = [];window.addEventListener('error',(e)=>{const entry ={type: 'js_error',msg: e.message,src: e.filename,line: e.lineno,ts: Date.now()};ERROR_LOG.push(entry);console.error('[Sovereign Guard] Exception:',entry);try{localStorage.setItem('ka_error_log',JSON.stringify(ERROR_LOG.slice(-20)));}catch(_){}return false;});window.addEventListener('unhandledrejection',(e)=>{const entry ={type: 'promise_rejection',msg: String(e.reason),ts: Date.now()};ERROR_LOG.push(entry);console.error('[Sovereign Guard] Unhandled Promise:',entry);try{localStorage.setItem('ka_error_log',JSON.stringify(ERROR_LOG.slice(-20)));}catch(_){}});const reveals = document.querySelectorAll('.reveal');try{const revealObs = new IntersectionObserver((entries)=>{entries.forEach((e,i)=>{if(e.isIntersecting){setTimeout(()=> e.target.classList.add('visible'),i * 80);revealObs.unobserve(e.target);}});},{threshold: 0.01,rootMargin: '0px 0px -50px 0px'});reveals.forEach(el => revealObs.observe(el));}catch(err){console.warn('[Sovereign Guard] ScrollReveal Observer Failed — Triggering Manual Reveal');reveals.forEach(el => el.classList.add('visible'));}setTimeout(()=>{document.querySelectorAll('.reveal:not(.visible)').forEach(el =>{el.classList.add('visible');console.log('[Sovereign Guard] Safety Reveal Triggered for:',el);});},2500);const nav = document.getElementById('mainNav');const hamburger = document.getElementById('hamburger');const navLinks = document.getElementById('navLinks');if(nav){window.addEventListener('scroll',()=>{if(window.scrollY > 80){nav.classList.add('scrolled');}else{nav.classList.remove('scrolled');}});}if(hamburger && navLinks){hamburger.addEventListener('click',(e)=>{e.stopPropagation();hamburger.classList.toggle('active');navLinks.classList.toggle('active');document.body.classList.toggle('no-scroll');});navLinks.querySelectorAll('a').forEach(link =>{link.addEventListener('click',()=>{hamburger.classList.remove('active');navLinks.classList.remove('active');document.body.classList.remove('no-scroll');});});document.addEventListener('click',(e)=>{if(!nav.contains(e.target)&& navLinks.classList.contains('active')){hamburger.classList.remove('active');navLinks.classList.remove('active');document.body.classList.remove('no-scroll');}});}const tabBtns = document.querySelectorAll('.tab-btn');const tabs = document.querySelectorAll('.fp-tab');if(tabBtns.length > 0){tabBtns.forEach(btn =>{btn.addEventListener('click',()=>{const target = btn.dataset.tab;const targetEl = document.getElementById(target);tabBtns.forEach(b => b.classList.remove('active'));tabs.forEach(t => t.classList.remove('active'));btn.classList.add('active');if(targetEl){targetEl.classList.add('active');targetEl.querySelectorAll('.reveal').forEach(el =>{el.classList.remove('visible');setTimeout(()=> el.classList.add('visible'),100);});}});});}document.querySelectorAll('a[href*="#"]').forEach(anchor =>{anchor.addEventListener('click',(e)=>{const href = anchor.getAttribute('href');const hash = href.substring(href.indexOf("#"));const path = href.substring(0,href.indexOf("#"));if(path === "" || path === "/" || path === window.location.pathname){const target = document.querySelector(hash);if(target){e.preventDefault();const offset = 100;const top = target.getBoundingClientRect().top + window.scrollY - offset;window.scrollTo({top,behavior: 'smooth'});if(navLinks.classList.contains('active')){hamburger.classList.remove('active');navLinks.classList.remove('active');document.body.classList.remove('no-scroll');}}}});});const ribbon = document.getElementById('stickyRibbon');if(ribbon && nav){let ribbonVisible = true;window.addEventListener('scroll',()=>{if(window.scrollY > 200 && ribbonVisible){ribbon.style.transform = 'translateY(-100%)';nav.style.top = '10px';ribbonVisible = false;}else if(window.scrollY <= 200 && !ribbonVisible){ribbon.style.transform = 'translateY(0)';nav.style.top = '44px';ribbonVisible = true;}});ribbon.style.transition = 'transform 0.4s cubic-bezier(0.4,0,0.2,1)';nav.style.transition = 'top 0.4s cubic-bezier(0.4,0,0.2,1),background 0.4s,border 0.4s';}const track = document.querySelector('.stats-track');if(track){track.innerHTML += track.innerHTML;}const forms = document.querySelectorAll('.sovereign-form-logic');const modal = document.getElementById('enquiryModal');const closeModal = document.getElementById('closeModal');document.querySelectorAll('.btn-modal,.cta-pill,.btn-primary,.btn-secondary,.nav-links a,.ribbon-cta').forEach(btn =>{btn.addEventListener('click',(e)=>{const text =(btn.innerText || btn.textContent).toLowerCase();if(text.includes('enquire')|| text.includes('visit')|| text.includes('price')|| text.includes('access')|| text.includes('roi')|| text.includes('calc')){if(modal){e.preventDefault();const modalHeader = modal.querySelector('.modal-header h3');const modalDesc = modal.querySelector('.modal-header p');if(modalHeader){if(text.includes('roi')|| text.includes('growth')){modalHeader.innerHTML = 'Request <span class="gold">ROI Analysis</span>';if(modalDesc)modalDesc.innerText = 'Unlock the complete market whitepaper and capital appreciation projection.';}else if(text.includes('price')){modalHeader.innerHTML = 'Get <span class="gold">Price List</span>';if(modalDesc)modalDesc.innerText = 'Receive the latest inventory status and pre-launch pricing directly on WhatsApp.';}else{modalHeader.innerHTML = 'Unlock <span class="gold">Privilege Access</span>';if(modalDesc)modalDesc.innerText = 'Enter your details to receive the official brochure and priority site visit slots.';}}modal.classList.add('open');trackEvent('Engagement','Modal Opened',text.trim());}}});});if(closeModal){closeModal.addEventListener('click',()=> modal?.classList.remove('open'));}window.addEventListener('click',(e)=>{if(e && modal && e.target === modal)modal.classList.remove('open');});forms.forEach(form =>{form.addEventListener('submit',function(e){e.preventDefault();const currentBtn = form.querySelector('.submit-btn');if(!currentBtn)return;const currentBtnText = currentBtn.querySelector('span')|| currentBtn;const name = form.querySelector('input[name="name"]');const phone = form.querySelector('input[name="phone"]');const config = form.querySelector('select[name="config"]');let valid = true;[name,phone].forEach(el => el && el.classList.remove('error'));if(config)config.classList.remove('error');const cleanedPhone =(phone?.value || '').replace(/\s+/g,'').replace('+','');const isDummy = /^(\d)\1{9,}$/.test(cleanedPhone);const phoneRegex = /^[0-9]{10,14}$/;if(!name || !name.value.trim()|| name.value.length < 2){if(name)name.classList.add('error');valid = false;}if(!phone || !cleanedPhone || !phoneRegex.test(cleanedPhone)|| isDummy){if(phone)phone.classList.add('error');valid = false;}if(!valid){shake(currentBtn);return;}const hp = form.querySelector('input[name="contact_me"]');if(hp && hp.checked){console.warn('[Sovereign Guard] Bot detected.');return;}const now = Date.now();let subHistory = JSON.parse(localStorage.getItem('ka_sub_history')|| '[]');subHistory = subHistory.filter(t => now - t < 60000);if(subHistory.length >= 2){alert('🔒 Security Protocol: Too many requests. Please wait a minute before submitting again.');return;}subHistory.push(now);localStorage.setItem('ka_sub_history',JSON.stringify(subHistory));currentBtn.disabled = true;const originalText = currentBtnText.textContent;currentBtnText.textContent = '⏳ DISPATCHING...';const data ={name: name.value.trim(),phone: cleanedPhone,email: form.querySelector('input[name="email"]')?.value.trim()|| 'N/A',config: config ? config.value : 'N/A',budget: form.querySelector('select[name="budget"]')?.value || 'N/A',message: form.querySelector('textarea[name="message"]')?.value.trim()|| 'N/A',_subject: 'New Strategic Lead — Krisala Aventis'};persistLead(data);const WEB3FORMS_KEY = 'b28972bc-8e15-4fe5-86b7-82b12ee0e82b';const MAX_RETRIES = 3;const RETRY_DELAY = 2000;const leadData ={access_key: WEB3FORMS_KEY,subject: `New Lead: ${data.name}— Krisala Aventis`,from_name: 'Krisala Aventis Live Portal',replyto: data.email !== 'N/A' ? data.email : undefined,...data};async function dispatchWithRetry(url,payload,retries = MAX_RETRIES){for(let attempt = 1;attempt <= retries;attempt++){try{const controller = new AbortController();const timeoutId = setTimeout(()=> controller.abort(),10000);const response = await fetch(url,{method: 'POST',headers:{'Content-Type': 'application/json','Accept': 'application/json'},body: JSON.stringify(payload),signal: controller.signal});clearTimeout(timeoutId);if(response.ok){const result = await response.json();console.log(`[Sovereign Pipeline] Relay SUCCESS(attempt ${attempt}):`,result);trackEvent('Pipeline','Email Delivered',`Attempt ${attempt}`);return{success: true,result};}throw new Error(`HTTP ${response.status}`);}catch(err){console.warn(`[Sovereign Pipeline] Attempt ${attempt}/${retries}failed:`,err.message);if(attempt < retries){await new Promise(r => setTimeout(r,RETRY_DELAY * attempt));}}}return{success: false};}dispatchWithRetry('https://api.web3forms.com/submit',leadData).then(result =>{if(result.success){markVaultDelivered(data);}else{queueFailedLead(data);console.error('[Sovereign Pipeline] All retries exhausted. Lead queued for recovery.');trackEvent('Pipeline','Email Failed - Queued',data.name);}showSuccess(currentBtn,currentBtnText,originalText,form,data.name,data.phone);form.reset();});});form.querySelectorAll('select').forEach(sel =>{sel.addEventListener('keydown',(e)=>{if(e.key === 'Enter')e.preventDefault();});});});if(modal){setTimeout(()=>{const isAnyModalOpen = document.querySelector('.modal-overlay.open');if(!isAnyModalOpen){modal.classList.add('open');const modalHeader = modal.querySelector('.modal-header h3');const modalDesc = modal.querySelector('.modal-header p');if(modalHeader)modalHeader.innerHTML = 'Unlock <span class="gold">Exclusive Offers</span>';if(modalDesc)modalDesc.innerText = 'Register now to receive current inventory status and pre-launch pricing.';console.log('[Sovereign Guard] Auto-Modal Triggered');}},1500);}function persistLead(data){try{const vault = JSON.parse(localStorage.getItem('ka_sovereign_vault')|| '[]');vault.push({...data,status: 'pending',timestamp: new Date().toISOString()});localStorage.setItem('ka_sovereign_vault',JSON.stringify(vault.slice(-100)));}catch(err){console.warn('Vault error:',err);}}function markVaultDelivered(data){try{const vault = JSON.parse(localStorage.getItem('ka_sovereign_vault')|| '[]');const match = vault.find(v => v.phone === data.phone && v.status === 'pending');if(match)match.status = 'delivered';localStorage.setItem('ka_sovereign_vault',JSON.stringify(vault));}catch(_){}}function queueFailedLead(data){try{const queue = JSON.parse(localStorage.getItem('ka_retry_queue')|| '[]');queue.push({...data,attempts: 0,timestamp: new Date().toISOString()});localStorage.setItem('ka_retry_queue',JSON.stringify(queue.slice(-20)));}catch(_){}}(function flushRetryQueue(){try{const queue = JSON.parse(localStorage.getItem('ka_retry_queue')|| '[]');if(queue.length === 0)return;console.log(`[Sovereign Pipeline] Flushing ${queue.length}queued lead(s)...`);const remaining = [];queue.forEach(lead =>{if(lead.attempts >= 5){console.warn('[Sovereign Pipeline] Lead exceeded max retry. Archived:',lead.name);return;}lead.attempts =(lead.attempts || 0)+ 1;const payload ={access_key: 'b28972bc-8e15-4fe5-86b7-82b12ee0e82b',subject: `[RETRY #${lead.attempts}] Lead: ${lead.name}— Krisala Aventis`,from_name: 'Krisala Aventis Retry System',...lead};fetch('https://api.web3forms.com/submit',{method: 'POST',headers:{'Content-Type': 'application/json','Accept': 'application/json'},body: JSON.stringify(payload)}).then(r =>{if(r.ok){console.log(`[Sovereign Pipeline] Retry delivered: ${lead.name}`);}else{remaining.push(lead);}}).catch(()=> remaining.push(lead));});setTimeout(()=>{localStorage.setItem('ka_retry_queue',JSON.stringify(remaining));},5000);}catch(_){}})();function showSuccess(btn,btnTextEl,originalText,form,nameVal,phoneVal){btn.disabled = false;const formTitle = document.getElementById('formTitle');const ppContainer = document.getElementById('priorityPassContainer');const ppName = document.getElementById('pp-name');const ppId = document.getElementById('pp-id');const qrImg = ppContainer?.querySelector('.pass-qr img');if(form && ppContainer && ppName && ppId){form.style.display = 'none';if(formTitle)formTitle.style.display = 'none';const day = form.querySelector('select[name="visit-day"]')?.value || 'TBD';const slot = form.querySelector('select[name="visit-slot"]')?.value || 'TBD';const ppSlot = document.getElementById('pp-slot');if(ppSlot)ppSlot.innerText = `${day}| ${slot}`;ppName.innerText = nameVal || 'Valued Guest';const passId = 'KA-' + Math.random().toString(36).substr(2,9).toUpperCase();ppId.innerText = passId;if(qrImg){qrImg.src = `https://api.qrserver.com/v1/create-qr-code/?size=150x150&data=PassID:${passId}|Slot:${day}-${slot}|Phone:${phoneVal}`;}ppContainer.style.display = 'block';ppContainer.style.animation = 'fadeIn 0.8s ease forwards';const confirmBtn = document.getElementById('confirmWA');if(confirmBtn){const waMsg = encodeURIComponent(`Hi,I have generated my Krisala Aventis Priority Pass(${passId}). \nSlot: ${day}| ${slot}. \nPlease confirm my visit.`);confirmBtn.href = `https://api.whatsapp.com/send?phone=917744009295&text=${waMsg}`;}trackEvent('Conversion','Priority Pass Generated',`${passId}| ${day}-${slot}`);}else{const originalBg = btn.style.background;const originalColor = btn.style.color;btn.style.background = 'var(--clr-gold)';btn.style.color = '#000';btnTextEl.textContent = '🏠 Enquiry Protocol Delivered!';setTimeout(()=>{btn.style.background = originalBg;btn.style.color = originalColor;btnTextEl.textContent = originalText;},5000);}}function shake(el){if(!el)return;el.style.animation = 'none';requestAnimationFrame(()=>{el.style.animation = 'shake 0.4s ease';});}const sections = document.querySelectorAll('section[id]');const navAnchors = document.querySelectorAll('.nav-links a[href^="#"]');if(sections.length > 0 && navAnchors.length > 0){const sectionObs = new IntersectionObserver((entries)=>{entries.forEach(e =>{if(e.isIntersecting){const id = e.target.id;navAnchors.forEach(a =>{a.style.color = a.getAttribute('href')=== `#${id}`
 ? 'var(--clr-gold)' : '';});}});},{threshold: 0.4});sections.forEach(s => sectionObs.observe(s));}document.querySelector('.logo')?.addEventListener('click',(e)=>{if(e.target.getAttribute('href')=== '#' || e.target.closest('a').getAttribute('href')=== '#'){e.preventDefault();window.scrollTo({top: 0,behavior: 'smooth'});}});function trackEvent(category,action,label){if(window.dataLayer && typeof window.dataLayer.push === 'function'){window.dataLayer.push({'event': 'ka_engagement','event_category': category,'event_action': action,'event_label': label});console.log(`[Analytics] Tracked: ${category}| ${action}| ${label}`);}}document.querySelectorAll('.wa-float,.wa-fab').forEach(waBtn =>{waBtn.addEventListener('click',()=>{trackEvent('Communication','WhatsApp Click',waBtn.classList.contains('wa-fab')? 'Floating FAB' : 'Footer/Ribbon');});});document.querySelectorAll('.btn-primary,.btn-secondary,.cta-pill,.ribbon-cta').forEach(btn =>{btn.addEventListener('click',()=>{const text = btn.innerText || btn.textContent;trackEvent('Engagement','Button Click',text.trim());});});const shakeStyle = document.createElement('style');shakeStyle.textContent = `
 @keyframes shake{0%,100%{transform: translateX(0);}20%{transform: translateX(-8px);}40%{transform: translateX(8px);}60%{transform: translateX(-5px);}80%{transform: translateX(5px);}}`;document.head.appendChild(shakeStyle);const connStyle = document.createElement('style');connStyle.textContent = `
 .share-btn-fp{margin-top: 1rem;display: inline-flex;align-items: center;gap: 8px;font-size: 0.85rem;color: var(--clr-gold);cursor: pointer;opacity: 0.8;transition: 0.3s;}.share-btn-fp:hover{opacity: 1;transform: translateX(5px);}`;document.head.appendChild(connStyle);document.querySelectorAll('.fp-details').forEach(details =>{const shareBtn = document.createElement('div');shareBtn.className = 'share-btn-fp';shareBtn.innerHTML = '<span>📲 Share Layout</span>';shareBtn.onclick =()=>{const bhk = details.querySelector('h3')?.innerText || '2/3 BHK';const msg = encodeURIComponent(`Check out this ${bhk}layout at Krisala Aventis Tathawade! It looks perfect. \n\nView here: ${window.location.href}`);window.open(`https://api.whatsapp.com/send?text=${msg}`,'_blank');};details.appendChild(shareBtn);});const prefetchLinks =()=>{const links = document.querySelectorAll('a[href^="/"]');const observer = new IntersectionObserver((entries)=>{entries.forEach(entry =>{if(entry.isIntersecting){const href = entry.target.getAttribute('href');if(href && !href.includes('#')){const link = document.createElement('link');link.rel = 'prefetch';link.href = href;document.head.appendChild(link);observer.unobserve(entry.target);}}});},{threshold: 0.1});links.forEach(link => observer.observe(link));};if('IntersectionObserver' in window){setTimeout(prefetchLinks,2000);}window.addEventListener('online',()=>{console.log('[Sovereign Guard] Connection restored. Flushing retry queue...');try{const queue = JSON.parse(localStorage.getItem('ka_retry_queue')|| '[]');if(queue.length > 0){queue.forEach(lead =>{fetch('https://api.web3forms.com/submit',{method: 'POST',headers:{'Content-Type': 'application/json','Accept': 'application/json'},body: JSON.stringify({access_key: 'b28972bc-8e15-4fe5-86b7-82b12ee0e82b',subject: `[AUTO-RECOVERY] Lead: ${lead.name}— Krisala Aventis`,from_name: 'Krisala Aventis Auto-Recovery',...lead})}).then(r =>{if(r.ok)console.log(`[Sovereign Guard] Auto-recovered lead: ${lead.name}`);}).catch(()=>{});});localStorage.setItem('ka_retry_queue','[]');}}catch(_){}});window.addEventListener('offline',()=>{console.warn('[Sovereign Guard] Connection lost. Leads will be queued locally.');});console.log(`[Krisala Aventis] Sovereign Intelligence v${SOVEREIGN_VERSION}— TOTAL HARDENING ACTIVE ✅`);if('serviceWorker' in navigator){window.addEventListener('load',()=>{navigator.serviceWorker.register('/sw.js').then(reg => console.log('[Service Worker] Sovereign Registration Successful ✅',reg.scope)).catch(err => console.warn('[Service Worker] Sovereign Registration Failed ❌',err));});}})();